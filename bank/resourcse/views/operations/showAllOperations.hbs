{{>header.layouts  }}
{{>nav.layouts  }}

<div class="container">
<table class="table table-striped ">
  <thead>
    <tr>
      <th scope="col">Amount</th>
      <th scope="col">Date</th>
      <th scope="col">Location</th>
        <th scope="col">Actions</th>
    </tr>
  </thead>
  <tbody>
      {{#each operations}}
    <tr>
    
      <td>{{this.amount}}</td>
      <td>{{this.date}}</td>
       <td>{{this.location}}</td>
     <td>
       <a href="/singleOperation/{{this._id}}" class="btn btn-primary">Show</a>
       <a href="/deleteOperation/{{this._id}}" class="btn btn-danger">Delete</a>
       <a href="/updateOperation/{{this._id}}" class="btn btn-success">Update</a>


     </td>
     
    </tr>
    {{/each}}
  </tbody>
</table>
<a href="/operations" class="btn btn-primary ">Add New Opertaion</a>
</div>
{{>footer.layouts}}